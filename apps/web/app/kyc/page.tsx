import { ComingSoon } from "@/components/coming-soon";

export default function KycPage() {
  return (
    <ComingSoon
      title="Tenant Verification (KYC)"
      phase="Phase 6 — KYC"
      description="Document upload and verification status for PAN, Aadhaar, and business documents will live here. The database schema (kyc_verifications) already exists; the flow itself is not built yet."
    />
  );
}
