import { ComingSoon } from "@/components/coming-soon";

export default function LeasePage() {
  return (
    <ComingSoon
      title="Lease Generation"
      phase="Phase 7 — Lease Generation"
      description="Lease drafting, an AI-generated plain-language summary, and digital signing will live here. The database schema (leases, lease_versions) already exists; the flow itself is not built yet."
    />
  );
}
